name: 'Source: sedo_net_new'

on:
  # Scheduling moved to Vercel Cron (dashboard/app/api/cron/sedo-net-new) which
  # dispatches this workflow at 6 AM / 1 PM / 7 PM ET — far more punctual than
  # GitHub's `schedule:` (which ran 30-75 min late). sources.yaml keeps the
  # canonical schedule_utc "0 10,17,23 * * *" as documentation of the cadence.
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: source-sedo_net_new
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - name: Run sedo_net_new
        env:
          SLACK_BOT_TOKEN:             ${{ secrets.SLACK_BOT_TOKEN }}
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
          SLACK_CHANNEL_SNAP:          ${{ vars.SLACK_CHANNEL_SNAP }}
          SUPABASE_NAMING_URL:         ${{ secrets.SUPABASE_NAMING_URL }}
          SUPABASE_NAMING_SERVICE_KEY: ${{ secrets.SUPABASE_NAMING_SERVICE_KEY }}

        run: pipeline run sedo_net_new
      - name: Commit state changes
        if: success()
        uses: ./.github/actions/commit-state
        with:
          message: "sedo_net_new: state $(date -u +%Y-%m-%dT%H:%M:%SZ)"
